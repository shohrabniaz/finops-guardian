# FinOps Guardian

Azure cost leak detector — idle VMs, orphaned disks, missing tags.

**Phase 2 — Week 8.** Deploy API to Heroku; scan via Azure Cost Management APIs.

## Phase 1

- Register Azure app with `CostManagementReader`
- Implement detectors in `src/detectors.py` (Phase 2)
